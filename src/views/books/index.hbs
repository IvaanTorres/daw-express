<div class="l-grid gap-3">
  {{#if books}}
    {{#each books}}
        <div class='card card-body'>
          <h2>Title: {{title}}</h3>
          <p>Author: {{author.name}}</p>
          <p>Price: {{price}}€</p>
          <a class="btn btn-primary" href="/books/{{_id}}">Open</a>
        </div>
    {{/each}}
  {{/if}}
</div>